import { Badge, Box, Button, Center, Image, Text, Wrap, WrapItem } from '@chakra-ui/react'
import React, { useEffect } from 'react'
// import { IoCalendarOutline, IoFastFoodOutline, IoLocationOutline, IoTrashOutline } from 'react-icons/io5'
// import { useDispatch, useSelector } from 'react-redux'
// import { getPublicSchedules } from '../redux/schedules/actions'
// import Logo from '../images/logo.png'
// import DetailCard from './DetailCard'

// function ListCard() {
//   // @ts-ignore
//   const { publicItems, isLoading, isError, error } = useSelector((state) => state.schedules)
//   const dispatch = useDispatch()
//   useEffect(() => {
//     // @ts-ignore
//     dispatch(getPublicSchedules())
//   }, [dispatch])
//   return (
//     <Wrap justify="center">
//       {publicItems.map((schedule) => (
//         <DetailCard key={schedule.id} detail={schedule.User.Detail} />
//       ))}
//     </Wrap>
//   )
// }

// export default ListCard
